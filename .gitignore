node_modules/
dist/
*.log
.DS_Store

# Whisper.cpp build + model cache (lazy-built on first audio message)
listener/.whisper/

# Runtime data
data/
agents/*/logs/
agents/*/sessions/
agents/*/.*.lock

# Credentials and env (local only, never commit)
agents/*/credentials.json
.env

# Build artifacts
.wrangler/
